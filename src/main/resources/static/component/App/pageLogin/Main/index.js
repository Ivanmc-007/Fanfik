import React from "react";
import FormLogin from "../FormLogin/index";

export default function Main() {
   return (
      <main>
         <section>
            <FormLogin />
         </section>
      </main>
   );
}