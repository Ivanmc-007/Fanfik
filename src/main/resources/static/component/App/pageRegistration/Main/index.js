import React from "react";
import FormRegistration from "../FormRegistration/index";
import "./style.scss"

export default function Main() {
   return (
      <main>
         <section className="section-one">
            <FormRegistration />
         </section>
      </main>
   );
}